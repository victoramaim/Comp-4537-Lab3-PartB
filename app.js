const mo = require('./modules/math');

console.log(`Hello Victor Vasconcellos. The result of the addition is = ${mo.add(2, 1)}`); // 3
console.log(`Hello Victor Vasconcellos. The result of the subtraction is = ${mo.subtract(2, 1)}`); // 1
